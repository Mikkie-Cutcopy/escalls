- render 'layouts/workers_tab'
div class='tab-pane active' id='users'
  div.page-header
    h2.page-header-title Workers
  div.tab-pane-content
    - unless @new_workers.empty?
      h3
        | Новые пользователи
      = render partial: 'new_worker', collection: @new_workers
    - unless @workers.empty?
      h3
        | Сотрудники
      = render partial: 'worker', collection: @workers



